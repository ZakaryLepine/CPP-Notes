Ellipsis allows functions to take an arbitrary number of parameters to a function.

```
return_type function_name(argument_list, ...)
```

Functions that use ellipsis must have at least one non-ellipsis parameter.